---
name: path-typo
description: Reviews React components for accessibility. Use when the user asks about a11y in a component.
paths: src/component/**/*.tsx
---

The directory is `components`, not `component`, so this never auto-loads.
