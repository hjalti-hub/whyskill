---
name: truncated-triggers
description: This skill contains an extended reference for our internal deployment pipeline, covering the build matrix, the artifact registry layout, the canary rollout percentages, the rollback procedure, and the on-call escalation path. This skill contains an extended reference for our internal deployment pipeline, covering the build matrix, the artifact registry layout, the canary rollout percentages, the rollback procedure, and the on-call escalation path. This skill contains an extended reference for our internal deployment pipeline, covering the build matrix, the artifact registry layout, the canary rollout percentages, the rollback procedure, and the on-call escalation path. This skill contains an extended reference for our internal deployment pipeline, covering the build matrix, the artifact registry layout, the canary rollout percentages, the rollback procedure, and the on-call escalation path. This skill contains an extended reference for our internal deployment pipeline, covering the build matrix, the artifact registry layout, the canary rollout percentages, the rollback procedure, and the on-call escalation path. This skill contains an extended reference for our internal deployment pipeline, covering the build matrix, the artifact registry layout, the canary rollout percentages, the rollback procedure, and the on-call escalation path. This skill contains an extended reference for our internal deployment pipeline, covering the build matrix, the artifact registry layout, the canary rollout percentages, the rollback procedure, and the on-call escalation path. This skill contain
when_to_use: Use when the user asks to deploy, cut a release, or roll back production.
---

The trigger phrases sit past the 1,536-character cap, so Claude never sees them.
